# SEO/AEO 日报生成规范

## 目标

每天检查配置的 SEO 资讯源，只对新增文章生成中文情报报告。报告必须帮助读者快速判断“发生了什么、为什么重要、能沉淀什么方法论”，而不是翻译标题或拼接网页结构。

## 内容采集要求

1. 优先使用各来源 RSS Feed 发现文章，避免栏目页的导航、广告和推荐模块污染。
2. Search Engine Land 使用 Feed 内完整正文；其他来源优先抓详情页正文。
3. 详情页正文优先取 Article/NewsArticle/BlogPosting 的 `articleBody`，其次取语义化 `<article>` 内容。
4. Moz 等详情页被 403 阻断时只保留 Feed 摘要，并标记为 `summary_only`，不得假装已经阅读全文。
5. Search Engine Roundtable 的 Daily Recap、活动推广、广告政策和低相关产品小改动不得自动升级为深度文章。
6. 已成功分析的 URL 才写入去重状态；模型或报告生成失败时不得标记为已处理，以便下次重试。
7. 报告必须区分“今日新发布”和“历史漏抓回补”。

## AI 筛选要求

1. 使用 GitHub Models 对文章进行统一分类，最多选择 3 篇“今日必读”。
2. 分类维度包含：内容类型、SEO/AEO 相关度、是否能形成独立方法论、是否只是快讯或推广。
3. 不能因为标题出现 AI、SEO、Google 等关键词就机械提高优先级。
4. 低价值但值得知晓的内容进入“低价值快讯”；无关、重复或纯推广内容直接过滤。
5. 中文标题和一句话说明必须是真正的中文编辑结果，不得保留整段英文摘要。

## 深度解读要求

每篇必读文章必须包含：

1. 中文标题与英文原文链接。
2. 一句话结论。
3. 文章解决的问题。
4. 2-5 个解释因果关系、前提或影响的核心观点。
5. 由本文独有观点推导出的具名方法论和检查维度。
6. 与方法论直接相关的监测指标。
7. 容易出现的错误理解。
8. 最终判断、适用对象、内容类型和阅读价值。

必须区分原文事实、作者观点和方法论转化，不得编造数据、案例或 Google 官方规则。

## 禁止输出

- 英文 Meta Description 直接充当摘要。
- “文章结构重点”以及网页导航、广告、推荐文章标题。
- 根据关键词套用固定的 AEO、FAQ、Schema、技术 SEO 或内链模板。
- 重复的“执行动作”“做一个实验”“2-4 周后复盘”。
- 对新闻快讯强行生成方法论。
- 把作者观点写成 Google 官方结论。
- 中英文半句拼接、截断句子或 UTC ISO 时间。

## 报告结构

1. 今日概览。
2. 今日关键信号。
3. 今日必读。
4. 中文深度解读。
5. 低价值快讯。
6. 抓取状态。

抓取异常放在报告末尾的状态区，不得占据报告开头。

## 失败策略

- 缺少模型令牌、模型调用失败或中文质量校验失败时，整次运行失败且不更新去重状态。
- 不允许在失败时退回旧版通用模板报告。
- Feed 可用但详情页失败时，可以用摘要参与分类，但通常不得进入深度解读，除非摘要足以支撑且模型明确降低证据强度。
